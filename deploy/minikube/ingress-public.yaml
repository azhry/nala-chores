apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: runner-manager-public
  namespace: agent-runner
spec:
  ingressClassName: nginx
  rules:
    - host: chores.nalanirvana.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: runner-manager
                port:
                  name: http
